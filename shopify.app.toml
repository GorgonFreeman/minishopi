# Shopify CLI app config — extension preview and deploy.
# Set `application_url` and `redirect_urls` to your public tunnel (same as HOST in `.env`).
# See https://shopify.dev/docs/apps/tools/cli/configuration

name = 'kitsuchan'
handle = 'kitsuchan'

client_id = '9714f468916471a3d6d6cb7b4fa0e777'

application_url = 'https://witness-cameron-aid-dock.trycloudflare.com/'
embedded = true

[build]
include_config_on_deploy = true
automatically_update_urls_on_dev = true

[access_scopes]
scopes = 'read_products,write_products'

[auth]
redirect_urls = [
  'https://witness-cameron-aid-dock.trycloudflare.com/auth/callback',
]

[pos]
embedded = false

[webhooks]
api_version = '2026-04'

[[webhooks.subscriptions]]
topics = ['app/uninstalled']
uri = '/webhooks/app/uninstalled'



