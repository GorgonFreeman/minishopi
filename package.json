{
  "name": "kitsuchan",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": "25"
  },
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "start": "node --env-file=.env server.js",
    "dev": "vite build && concurrently -k \"npm run build:watch\" \"npm start\"",
    "dev:auto": "node scripts/dev-auto.mjs",
    "dev:tabs": "node scripts/dev-tabs.mjs",
    "dev:full": "npm run dev:tabs",
    "shopify:dev": "node scripts/shopify-dev.mjs",
    "tunnel": "node scripts/tunnel-with-sync.mjs",
    "tunnel:raw": "cloudflared tunnel --url http://localhost:3121",
    "shopify:build": "npx --yes @shopify/cli@latest app build"
  },
  "dependencies": {
    "@shopify/polaris": "^13.9.5",
    "@shopify/shopify-api": "^13.0.0",
    "@upstash/redis": "^1.37.0",
    "cloudflared": "^0.7.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.14.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "concurrently": "^9.2.1",
    "ttab": "^0.9.0",
    "vite": "^6.3.5"
  }
}
