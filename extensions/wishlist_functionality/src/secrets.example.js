// Copy this file to secrets.js and fill in the real value.
// secrets.js is gitignored — never commit the real secret.
// NOTE: this value ends up in the compiled extension bundle and is visible
// in the admin browser. Keep the cloud function rate-limited accordingly.
export const WISHLIST_HMAC_SECRET = 'replace-me';
