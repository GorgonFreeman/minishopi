{
  "name": "minishopi",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "tunnel": "cloudflared tunnel --url http://localhost:3121"
  },
  "dependencies": {
    "@shopify/shopify-api": "^13.0.0",
    "cloudflared": "^0.7.1"
  }
}
